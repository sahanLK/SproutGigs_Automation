/*
When the submission items for particular items, display kind of a warning that there is nothing
to submit for that particular type of data.
*/

(function(message) {
    const element2 = $('body');

    for (var i=1; i<=2; i++) {
        $(element2).fadeOut(50);
        $(element2).fadeIn(200);
    }
})(arguments[0])